[toolchain]
# Pinned so that every contributor and CI run uses the same compiler.
# Bump deliberately, together with a benchmark re-run (see docs/BENCHMARKING.md).
channel = "1.98.0"
components = ["rustfmt", "clippy", "rust-src"]
profile = "minimal"
