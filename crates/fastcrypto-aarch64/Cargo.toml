[package]
name = "fastcrypto-aarch64"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "AArch64 backend (feature detection and SIMD primitives) for fastcrypto-rs"

[dependencies]

[features]
default = []
# Enables the platform feature probe; without it every feature reports as
# unavailable and callers fall back to the portable backends.
std = []

[lints]
workspace = true

